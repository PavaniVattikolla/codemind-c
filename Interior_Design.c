#include<stdio.h>
int main()
{
    int x,y,a,b,z,c;
    scanf("%d%d%d%d",&x,&y,&a,&b);
    z=x+y;
    c=a+b;
    if (z<c)
    {
        printf("%d",z);
    }
    else if (z>c)
    {
        printf("%d",c);
    }
    else if (x=c)
    {
        printf("%d",x);
    }
}